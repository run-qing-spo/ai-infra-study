# Environment evidence

Raw machine reports are excluded from Git. Public reports must omit SSH
addresses and preserve the distinction between guest-visible observations and
unknown host/cloud properties.
