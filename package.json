{
  "name": "ai-infer-io-opt",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "deploy": "vitepress build docs && gh-pages -d docs/.vitepress/dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "markdown-it-task-lists": "^2.1.1",
    "vitepress": "^1.0.0"
  }
}
